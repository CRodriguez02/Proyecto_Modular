CREATE TABLE administradores (
username varchar (50) PRIMARY KEY,
correo varchar (255) NOT NULL,
nombre varchar (100) NOT NULL,
apellido_paterno varchar (100) NOT NULL,
apellido_materno varchar (100),
contrasena varchar (50) NOT NULL
);

CREATE TABLE usuarios (
username varchar (50) PRIMARY KEY,
correo varchar (255) NOT NULL,
nombre varchar (100) NOT NULL,
apellido_paterno varchar (100) NOT NULL,
apellido_materno varchar (100),
contrasena varchar (50) NOT NULL,
imagen mediumblob
);

CREATE TABLE mascotas (
ID varchar (7) PRIMARY KEY,
titulo varchar (100) NOT NULL,
descripcion text,
imagen mediumblob,
estado tinyit NOT NULL,
especie varchar (100) NOT NULL,
raza varchar (100) NOT NULL,
CONSTRAINT fk_username FOREIGN KEY (usernameUsuario) REFERENCES usuarios (username),
recompensa tinyit NOT NULL
);

CREATE TABLE objeto (
ID varchar (7) PRIMARY KEY,
titulo varchar (100) NOT NULL,
descripcion text,
imagen mediumblob,
estado tinyit NOT NULL,
categoria varchar (100) NOT NULL,
CONSTRAINT fk_username FOREIGN KEY (usernameUsuario) REFERENCES usuarios (username),
recompensa tinyit NOT NULL
);

*****CREATE TABLE reportes (
folio varchar (12) PRIMARY KEY,
descripcion text,
CONSTRAINT fk_objeto FOREIGN KEY (ID_OBJ) REFERENCES objeto (ID),
CONSTRAINT fk_username FOREIGN KEY (usernameUsuario) REFERENCES usuarios (username)
);